# 《专业大逃杀》参考图竖切完成审计

> 日期：2026-07-17  
> 审计对象：`2026-07-17_参考图驱动的整体重构计划.md` 阶段 0～11  
> 结论：全部阶段已按顺序实施；计划内功能、视觉、回归和交付要求均有当前状态证据。

## 阶段证据

| 阶段 | 中文提交 | 当前状态直接证据 | 结论 |
| --- | --- | --- | --- |
| 0 稳定基线 | `d5bad46` | `tests/test_runner.gd` 覆盖五专业、108 卡、跨战斗生命/精神、AI 首回合；设置现为暂停覆盖层并保留同一战斗对象 | 通过 |
| 1 视觉基础 | `4a3ea49` | `src/ui/screens/style_guide.tscn`、`src/ui/theme.tres`、`progress/01_visual_foundation.png` | 通过 |
| 2 素材管线 | `8e31f3f` | `assets/art_manifest.json`；菜单、校园、教室、面试室背景与计算机玩家/敌人/卡面已接入 | 通过 |
| 3 主菜单/专业 | `4c72386` | `major_select.tscn` 同屏展示五专业、八维、技能、选中态与开局；键盘和鼠标入口均接通 | 通过 |
| 4 可移动校园 | `7e795f7` | `CampusPlayer` 使用 `CharacterBody2D`；场景含五组建筑碰撞、边界、前景遮挡与五热点；自动化验证移动、阻挡、交互和位置恢复 | 通过 |
| 5 探索 HUD | `6545eab` | 顶栏、资源、压力小地图、目标、事件面板、快捷键栏和世界压力区均读取真实局内状态 | 通过 |
| 6 普通战 | `7654b23` | 正式教室舞台；`BattleHandLayout` 自动化验证 3/5/7/10 手牌安全区；抽弃牌、能量、状态、意图和结束回合固定布局 | 通过 |
| 7 战斗反馈 | `17c5a9d` | 卡牌专业/类型/稀有度规则；抽牌、悬停、拒绝、攻击、受击、护盾、治疗、状态和胜负 Tween；关键卡牌数值抽样断言 | 通过 |
| 8 AI 精英战 | `285f16e` | AI 面试官与论文审稿人独立档案、掉落和白名单；测试覆盖离线、超时、非法行动回落与 UI 状态 | 通过 |
| 9 成长闭环 | `2c582d1` | 五类区域各有两项事件；奖励写入牌组、属性、遗物和资源；`full_run_runner.tscn` 验证奖励后回到同一局校园 | 通过 |
| 10 五专业美术 | `137c41a` | 五份玩家立绘、十份唯一敌人立绘、专业核心卡面和终局背景；程序动效覆盖待机、攻受击、状态与胜负 | 通过 |
| 11 整局交付 | `18f8c1f` | 独立整局测试从菜单推进到唯一上岸者；21 张三档视口截图；macOS/Windows 正式包与 43 秒演示视频 | 通过 |

## 最终完成标准核对

1. 夜景校园主界面和五专业选择：通过。
2. 等距校园移动及五类建筑互动：通过。
3. 教室普通卡牌战与完整反馈：通过。
4. AI Native 档案、候选行动、策略气泡和安全兜底：通过。
5. 奖励、牌组、属性、遗物和压力圈构成一局成长：通过。
6. Boss、唯一上岸者、成就和本局总结：通过。
7. 正式背景、五专业和十敌人均已映射；不再出现几何背景或火柴人角色：通过。

## 自动化和视觉门禁

```bash
.venv/bin/python -m pytest tests/ -v
tools/Godot.app/Contents/MacOS/Godot --headless --path . --editor --quit
tools/Godot.app/Contents/MacOS/Godot --headless --path . --scene tests/full_run_runner.tscn
git diff --check
```

- Python：13 项通过；唯一警告来自 Starlette 对 `httpx` 的第三方弃用提示。
- Godot 逻辑回归：数据、校园、交付界面、战斗、AI、局内状态和自定义专业全部通过。
- 整局回归：主菜单 → 五专业选择 → 五热点 → 普通战 → 奖励 → AI 战 → 奖励 → Boss → 唯一上岸者 → 总结 → 主菜单，通过。
- 分辨率矩阵：`progress/11_{画面}_{宽}x{高}.jpg` 共 21 张，覆盖七个核心画面与 1280×720、1600×900、1920×1080 三档；已检查裁切、遮挡和文字溢出。

## 构建与演示

- macOS：`build/mac/MajorRoyale.app`，Universal x86_64/arm64，签名结构验证通过，Bundle ID `com.majorroyale.game`，版本 `1.1.0`。
- Windows：`build/win/MajorRoyale.exe`，PE32+ x86-64；PCK 约 25 MB。
- 演示：`docs/demo-1.1.0-vertical-slice.mp4`，H.264，1920×1080，30 FPS，43 秒。
- `build/` 被 Git 忽略；构建产物保留在本地交付目录，不进入源码提交。

## 剩余边界

- Windows 包由 macOS 交叉构建，尚未在原生 Windows 主机启动；文件格式和导出流程已验证。
- 引擎参数 `--quit-after` 会强制跳过游戏内优雅退出并报告一个残留资源；正常菜单退出和两条自动化退出路径均先清理音频，未出现阻塞性错误。
- 当前角色表现采用正式静态立绘配合程序 Tween，而非逐帧动画图集；这属于版本边界，不影响本计划的可玩竖切完成标准。
